# Causal Mosaic Annotation Pipeline — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP client (validate_curies.py, lookup_curie.py)
requests>=2.31.0

# YAML parsing (all scripts)
pyyaml>=6.0.1

# CLI framework (all scripts)
click>=8.1.7

# LLM client — install the one that matches your llm.yaml provider:
#
#   provider: openai_compatible  (vLLM, Ollama, Together, Groq, OpenAI, etc.)
openai>=1.30.0
#
#   provider: anthropic
# anthropic>=0.40.0

# Optional — PDF text extraction (extract.py and verify_spans.py)
# Uncomment one of the following if your source documents are PDFs:
# pdfplumber>=0.10.0
# pymupdf>=1.23.0
